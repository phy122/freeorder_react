<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.freeorder.mapper.ProductMapper">

    <!-- 조회 -->
    <select id="select" resultType="Product">
        SELECT *
        FROM products
        WHERE id = #{id}
    </select>

    <!-- 목록 조회 -->
    <select id="allList" resultType="Product">
        SELECT *
        FROM products
        ORDER BY seq asc
    </select>

    <!-- 목록 조회 - category_id -->
    <select id="listByCate" resultType="Product">
        SELECT *
        FROM products
        WHERE category_id = #{categoryId}
    </select>

    <!-- 등록 -->
    <insert id="insert">
        INSERT INTO products ( name, category_id, price, seq )
        VALUES ( #{name}, #{categoryId}, #{price}, #{seq} )
    </insert>

    <!-- 수정 -->
    <update id="update">
        UPDATE products
           SET name = #{name}
              ,category_id = #{categoryId}
              ,price = #{price}
              ,stock_check = #{stockCheck}
              ,seq = #{seq}
              ,updated_at = now()
        WHERE id = #{id}
    </update>

    <!-- 삭제 -->
    <delete id="delete">
        DELETE FROM products
        WHERE id = #{id}
    </delete>
</mapper>